# AxonV Demo Project

To use just cd to build directory, and cmake ..
Put the file into the build directory to make it easier.
Then make and after that ./main ./input.pcd (file path)
Optionally include row spacing as the last argument.
